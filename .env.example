# PostgreSQL Database Configuration
POSTGRES_DB=kawait_prod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me

# Database URL para producción (Docker)
# Formato: postgres://usuario:password@host:puerto/database
DATABASE_URL=postgres://postgres:change_me@db:5432/kawait_prod

# Node Environment
NODE_ENV=production

# Frontend origin (used by backend CORS)
# IMPORTANTE: Agregá TODOS los orígenes desde donde vas a acceder
# Ejemplos:
# - Local: http://localhost:5173,http://localhost:3000
# - VPS con IP: http://tu.ip.publica,http://tu.ip.publica:3000
# - VPS con dominio: https://tudominio.com,http://tudominio.com
# Separar múltiples orígenes con comas (sin espacios)
CORS_ORIGINS=http://localhost,http://localhost:3000,http://localhost:5173

# Frontend API URL (used by frontend to connect to backend)
# Para Docker local: http://localhost:3001 o /api
# Para VPS con IP pública: http://tu.ip.publica:3001
# Para VPS con dominio: https://api.tudominio.com
VITE_API_URL=/api
